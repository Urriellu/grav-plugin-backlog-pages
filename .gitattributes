# Keep development-only files out of the archives GitHub generates for a
# release -- those archives are what GPM downloads and installs.
/.github     export-ignore
/tests       export-ignore
/.gitignore  export-ignore
/.gitattributes export-ignore
